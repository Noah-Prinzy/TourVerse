# Android application

Open the `androidApp` folder in Android Studio.

The Android emulator connects to the host computer backend through:

```text
http://10.0.2.2:8080
```

For a physical phone, replace this URL with your computer's local network IP address.

Start the Ktor backend before running the app.
