# Backend

## Run

Use IntelliJ IDEA or a local Gradle installation:

```bash
gradle run
```

The API starts at:

```text
http://localhost:8080
```

Test:

```bash
gradle test
```

This starter uses an in-memory repository so it can run before PostgreSQL is connected.
