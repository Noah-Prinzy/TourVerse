ktor {
    deployment {
        host = "0.0.0.0"
        port = 8081
        port = ${?PORT}
        shutdownGracePeriod = 3000
        shutdownTimeout = 5000
    }

    application {
        modules = [ com.tourverse.ApplicationKt.module ]
    }
}
